```mermaid
flowchart TD

Adafruit_GF(Adafruit_GFX(int16_t w, int16_t h))

drawPixel(drawPixel(int16_t x, int16_t y, uint16_t color))

startWrite(startWrite)
endWrite(endWrite)
writePixel(writePixel)
writeFillRect(writeFillRect)
writeFastVLine(writeFastVLine)
writeFastHLine(writeFastHLine)
writeLine(writeLine)

setRotation(setRotation)
invertDisplay(invertDisplay)

drawFastVLine(drawFastVLine)
drawFastHLine(drawFastHLine)
fillRect(fillRect)
fillScreen(fillScreen)
drawLine(drawLine)
drawRect(drawRect)


drawCircle(drawCircle)
drawCircleHelper(drawCircleHelper)
fillCircle(fillCircle)
fillCircleHelper(fillCircleHelper)
drawTriangle(drawTriangle)
fillTriangle(fillTriangle)
drawRoundRect(drawRoundRect)
fillRoundRect(fillRoundRect)
drawBitmap(drawBitmap)
drawXBitmap(drawXBitmap)
drawGrayscaleBitmap(drawGrayscaleBitmap)
drawRGBBitmap(drawRGBBitmap)
drawChar(drawChar)
getTextBounds(getTextBounds)
setTextSize(setTextSize)
setFont(setFont)
setCursor(setCursor)
setTextColor(setTextColor)
setTextWrap(setTextWrap)



  write(virtual size_t write(uint8_t))
```