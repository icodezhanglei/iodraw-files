```mermaid
flowchart LR
Adafruit_GF(Adafruit_GFX)
drawPixel(drawPixel)
startWrite(startWrite)
endWrite(endWrite)
writePixel(writePixel)
writeFillRect(writeFillRect)
writeFastVLine(writeFastVLine)
writeFastHLine(writeFastHLine)
writeLine(writeLine)
setRotation(setRotation)
invertDisplay(invertDisplay)
drawFastVLine(drawFastVLine)
drawFastHLine(drawFastHLine)
fillRect(fillRect)
fillScreen(fillScreen)
drawLine(drawLine)
drawRect(drawRect)
drawCircle(drawCircle)
drawCircleHelper(drawCircleHelper)
fillCircle(fillCircle)
fillCircleHelper(fillCircleHelper)
drawTriangle(drawTriangle)
fillTriangle(fillTriangle)
drawRoundRect(drawRoundRect)
fillRoundRect(fillRoundRect)
drawBitmap(drawBitmap)
drawXBitmap(drawXBitmap)
drawGrayscaleBitmap(drawGrayscaleBitmap)
drawRGBBitmap(drawRGBBitmap)
drawChar(drawChar)
getTextBounds(getTextBounds)
setTextSize(setTextSize)
setFont(setFont)
setCursor(setCursor)
setTextColor(setTextColor)
setTextWrap(setTextWrap)

writeLine --> writePixel --> drawPixel

writeFastVLine --> drawFastVLine --> startWrite & endWrite
drawFastVLine --> writeLine

writeFastHLine --> drawFastHLine --> startWrite & endWrite
drawFastHLine --> writeLine

writeFillRect --> fillRect --> writeFastVLine & startWrite & endWrite

fillScreen --> fillRect
drawLine --> drawFastVLine & drawFastHLine & startWrite & endWrite & writeLine
drawCircle --> startWrite & endWrite & writePixel
drawCircleHelper --> writePixel
fillCircle --> startWrite & endWrite & writeFastVLine & fillCircleHelper
fillCircleHelper --> writeFastVLine
drawRect -->



```